# Variable expansion examples
# First set some base variables
BASE_DIR=/usr/local
APP_NAME=myapp
DB_HOST=localhost
DB_PORT=5432

# Variables can reference previously defined ones
DATA_PATH=${BASE_DIR}/data
LOG_PATH=${BASE_DIR}/${APP_NAME}/logs

# Mixed variable expansion styles
DATABASE_URL=postgresql://${DB_HOST}:${DB_PORT}/mydb?sslmode=disable

# Variables can reference environment variables set outside the .env file
# (if HOME is set in the environment, it will be expanded)
USER_PATH=${HOME}/apps
